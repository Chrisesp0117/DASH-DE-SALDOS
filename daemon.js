#!/usr/bin/env node

/*
 * LEGACY FILE
 * This project now runs in serverless mode only.
 * Use Vercel API routes + cron-job.org + Telegram webhook.
 */

console.error('daemon.js is deprecated. Use serverless endpoints only.');
process.exit(1);
